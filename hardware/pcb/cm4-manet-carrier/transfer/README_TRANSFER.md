# Transfer notes for v0.8 handoff

Place or extract the current `v0.8` PCB project handoff here, or replace this file with the handoff notes from the Windows/Codex workflow.

Needed from the Windows/Codex side:

- KiCad project files: `.kicad_pro`, `.kicad_sch`, `.kicad_pcb`
- Local symbol libraries: `.kicad_sym`
- Local footprint libraries: `.pretty/`
- BOM files
- CPL / placement files
- Gerbers and drill files
- Datasheet and pinout source notes
- Short changelog: what `v0.8` contains, known issues, and next intended step

Do not rely on Gerbers alone as the editable source of truth; include the KiCad source files where possible.
